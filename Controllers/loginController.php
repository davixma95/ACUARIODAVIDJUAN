<?php
    /*if(session_status() == 1){
        session_start();
    }
    
    require_once 'Models/BBDDModel.php';
    
    if(isset($_POST['btn_acceder'])){
        $userPost = $_POST['user'];
        $password = $_POST['password'];
        $tabla = 'users';
        BBDDModel::crearConexion();
        $arrayUser = BBDDModel::select('*', $tabla);
        if($userPost == 'admin' && $password == 'admin'){
            $_SESSION['arrayUser'] = $arrayUser[0];
            require_once 'Controllers/adminController.php';
            die();
        }
        //EMPALMAR CON LA PÁGINA DE DAVID
        //require_once '';
        die();
    }
    
    require_once 'Views/loginView.php';
?>*/

